# Caesar Cipher Encryption

This project is a simple Python implementation of the **Caesar Cipher**, a basic encryption technique used to encode text by shifting each letter by a fixed number of positions in the alphabet. The program takes a message and a shift value from the user, then generates the encrypted text. It is a beginner-friendly project designed to demonstrate basic Python concepts such as functions, loops, strings, user input, and the use of the modulo operator.
